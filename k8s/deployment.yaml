apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "2Mi" 
            cpu: "50m"
          requests:
            memory: "1Mi"
            cpu: "25m"